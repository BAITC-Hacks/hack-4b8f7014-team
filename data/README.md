# Private runtime data

Created automatically: `uploads/` and `minutes.sqlite3`. Planned: `audio/`, `transcripts/`,
`exports/`. Everything except this file is ignored by Git. Back up the database and media
together. Establish retention and deletion procedures before using real meeting data.
