-r requirements-core.txt
# Local inference; weights are provisioned separately, never downloaded at runtime.
faster-whisper>=1.1,<2
pyannote.audio>=4,<5
ollama>=0.4,<1
python-docx>=1.1,<2
reportlab>=4.2,<5
